#include "glcompat/platform/android/android_capabilities.hpp"

namespace glcompat {
// AndroidCapabilities is header-only; this TU exists so the type has a stable
// object file and the platform abstraction is explicitly part of the library.
} // namespace glcompat
